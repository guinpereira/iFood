<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Botão Novo Produto -->
    <item
        android:id="@+id/menuNovoProduto"
        android:icon="@drawable/ic_adicionar_24dp"
        android:title="Novo Produto"
        app:showAsAction="always" />

    <!-- Botão Pedidos com badge -->
    <item
        android:id="@+id/menuPedido"
        android:title="Pedidos"
        app:showAsAction="always"
        app:actionLayout="@layout/badge_layout" />


    <!-- Search View na AppBar -->
    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_pesquisa_24dp"
        android:title="Pesquisar"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <!-- Configurações -->
    <item
        android:id="@+id/menuConfiguracoes"
        android:title="Configurações"
        app:showAsAction="never" />

    <!-- Sair -->
    <item
        android:id="@+id/menuSair"
        android:title="Sair"
        app:showAsAction="never" />

</menu>
